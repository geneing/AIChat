package com.eugene.aichat.core.data.db

/**
 * Reserved for future entities that store complex types directly.
 * Current entities use CSV strings to keep schema simple and avoid
 * JSON round-trips on hot paths. This stub keeps the @TypeConverters
 * surface stable for upcoming tables (e.g., multimodal content parts).
 */
class Converters
